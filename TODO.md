# TODO 列表

本文档记录 ShareSDU 项目的待办事项和未来计划。

## 🚀 Web 全面优化专项（2026-07）

> 目标：在不破坏现有业务能力的前提下，系统修复 Web 端正确性、性能、PC/移动端体验、无障碍与 Agent 可靠性问题。每个阶段完成后需通过构建、Lint、单元测试和关键页面回归。

### P0：正确性与路由

- [x] 修复文章编辑 `csEditArticle()` 使用未定义 `tags`（新旧 API 目录）
- [x] 修复 Agent 本轮用户消息重复注入上下文
- [x] 修复 `requiresAuth` 路由未校验登录态
- [x] 修复 Agent“清空聊天”仅清 UI、不清 IndexedDB 与会话记忆

### P1：首屏性能与依赖

- [x] Vuetify 改为按需组件/指令注册，移除重复全量 CSS
- [x] MDI Webfont 改为按需 SVG 图标，移除字体资源
- [x] AppMobile、AppDesktop、帖子编辑器、课程编辑器按需加载
- [x] 富文本、Markdown、图片裁剪、公式、海报等重型能力仅在对应路由加载
- [x] 增加首屏 JS/CSS 性能预算并接入 CI
- [ ] 图片补充尺寸、懒加载、响应式来源和现代格式策略（欢迎页二维码与备案图已补尺寸/懒加载；全站 WebP/AVIF、`srcset` 待分批迁移）
- [x] 移除移动端启动页及微服务页的人为等待
- [x] 路由组件直接使用 `() => import()`，移除 RouterView 的 `$route.fullPath` 强制重挂载

### P1：UI、交互与无障碍

- [x] 建立统一设计 Token（颜色、间距、圆角、阴影、容器、表单、状态色）
- [x] 优化欢迎页信息层级、主 CTA、标题语义与动画强度
- [x] 优化 Agent 页导航竞争、首次配置引导与 PC 阅读宽度
- [x] 移动端底部导航增加文字标签与清晰选中态
- [ ] 所有图标按钮补充可访问名称（App、欢迎页、Agent 和配置弹窗关键操作已完成；其余历史页面待审计）
- [x] 增加键盘发送、焦点环、Esc 关闭和跳过导航
- [ ] 删除/清空/退出/注销等危险操作统一确认与撤销策略（Agent 删除/清空已确认；退出和注销及统一撤销待完成）
- [ ] 统一空状态、错误状态、离线状态、重试入口与骨架屏
- [x] 欢迎页动画支持 `prefers-reduced-motion`
- [x] 设备状态监听窗口尺寸与屏幕旋转，兼顾 PC/移动端切换

### P1：Agent 编排、成本与安全

- [x] 支持流式输出与增量渲染
- [x] 意图路由增加规则快速路径，LLM 仅作兜底
- [x] 鉴权/网络失败不再错误回退到 Search Agent
- [x] `site_docs` 与 `site_query` 独立分支有限并发执行并统一综合
- [x] 同轮独立 Tool Call 有限并发
- [x] 工具调用支持超时、重试、退避与 AbortSignal
- [x] 增加总 Token、工具数、总耗时和结果字节预算
- [x] 使用 JSON Schema 校验工具参数和返回值
- [x] 工具结果裁剪、结构化、去 HTML 后再进入模型上下文
- [x] 明确站内内容为不可信数据，防御间接 Prompt Injection
- [x] PlanBuilder 实际约束预算与允许工具
- [x] 多分支结果增加最终综合层，处理重复、冲突与引用
- [x] Agent 配置增加连接测试、工具调用能力检测、模型列表与错误诊断
- [x] API Key 支持仅本次会话/持久保存两种模式并增加风险提示
- [x] 会话切换增加竞态保护，生成期间锁定或明确后台状态
- [x] 增加会话搜索、重命名、导出、全部删除和容量管理

### P2：工程质量与回归

- [x] 修复 ESLint 基线并为 Jest/Cypress 配置正确环境
- [x] 修复 Jest 配置，恢复单元测试执行
- [ ] 合并新旧 API 数据层并逐步移除 `src/axios`
- [ ] 拆分超大页面和 Agent UI/状态/协议代码
- [ ] 清理生产环境 `console`、`window.alert` 与固定延迟（本轮涉及的固定延迟与 Agent 危险操作已处理；历史页面待清理）
- [x] Debug 路由仅在开发环境注册
- [ ] 为认证、文章编辑、路由、Agent 会话和响应式导航补充回归测试（Agent 运行时/意图路由和现有工具测试已恢复，共 54 项；其余专项待补）

## 🔥 高优先级
### 功能优化
- [ ] 部分页面加载后的UI更新延迟问题（如主页帖子列表、板块列表）
- [ ] 微服务部分跳转提示逻辑
- [ ] 部分组件的加载视图和组件在加载完成时共存问题
- [ ] 登陆状态检测与控制优化
- [ ] 私聊页面的移动端UI优化，以及部分BUG
- [ ] 消息通知的点击跳转与状态更新逻辑优化
- [ ] 部分页面的状态保存与恢复完善(长期而言转为路由控制)

### 功能开发
- [ ] 搜索的多 tags 筛选功能
- [ ] 邮箱换绑功能
- [ ] 举报和申诉功能
- [ ] @艾特操作的 API（可以直接在 post/reply/article 加个字段值）

### 用户系统
- [ ] 管理员权限设置的完善（目前可以先用 mysql 改改）
- [ ] 贡献值/荣誉分等细节系统设计

## 📋 中优先级

### 功能完善
- [ ] 关注操作功能
- [ ] 注册码机制给没有山大邮箱的毕业校友

### 技术优化
- [ ] 缓存优化
- [ ] 索引优化
- [ ] 各种返回值的修正

## 📝 低优先级

### 文档
- [ ] 文档里的类之后可能会修改，需要保持同步更新

### 路由
- [x] 路由优化（2026-03-06 完成）
  - 添加路由过渡动画，解决页面切换空白问题
  - 优化异步组件加载延迟（delay=0）
  - 实现路由预加载机制
  - 更新相关文档 

## 💡 备忘录

### 开发注意事项
- `celery -A sdu_share_backend worker --beat` 不要忘记加 --beat
- 上线记得把 beat 和 worker 分开

### 已知问题
- 无

## 📊 进度追踪

### 已完成 ✅
- [x] 搜索 API 板块（Elasticsearch + django-haystack）
- [x] 置顶操作相关板块
- [x] 邀请码相关 API
- [x] 图片审核功能
- [x] 用户创作内容获取 API
- [x] 私信和聊天记录 API

### 进行中 🚧
- 无

### 已搁置 ⏸️
- 无

## 🎯 版本规划

### v1.6.0 计划
- 搜索多 tags 筛选
- 邮箱换绑
- 举报和申诉功能

### v1.7.0 计划
- @艾特操作
- 关注功能
- 贡献值/荣誉分系统

### v2.0.0 计划
- 大规模性能优化
- 缓存系统重构
- 管理员权限系统完善

## 📌 贡献指南

如果你想认领某个 TODO 项目：
1. 在对应项目前添加 `[认领人: 你的名字]`
2. 创建对应的 Issue 或 PR
3. 完成后更新状态到"已完成"

## 🔄 更新记录

- 2026-03-03: 创建 TODO 列表
- 从 api.md 迁移原有 TODO 项目
